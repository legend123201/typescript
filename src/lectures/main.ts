export function isPositive(n: number) {
  return n > 0;
}

/* 
- khi muốn biết mình đang ở client hay server, hãy xem "typeof window" có bằng undefined hay ko, 
ở client mới có biến window.

- truthy value và falsy value chỉ giá trị khi chuyển qua boolean của các kiểu dữ liệu để mình dùng if else. 
Lên mạng để xem cái bảng falsy value, cái nào ko có trong bảng đó thì sẽ là truthy value.

- cách đặt tên:
- tên hằng: UPPER_CASE
- tên biến và tên hàm: camelCase
*/
